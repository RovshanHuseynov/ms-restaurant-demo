databaseChangeLog:
  - changeSet:
      id: create-users-table
      author: Rovshan Huseynov
      changes:
        - createTable:
            tableName: users
            remarks: Table to store information about users
            columns:
              - column:
                  name: id
                  type: varchar(16)
                  constraints:
                    primaryKey: true
                    primaryKeyName: pk_users_tables
                    nullable: false
              - column:
                  name: name
                  type: varchar(128)
                  constraints:
                    nullable: false
              - column:
                  name: state
                  type: varchar(16)
                  constraints:
                    nullable: false
                  defaultValue: "ACTIVE"
                  remarks: enum with values ACTIVE, PAUSED, ARCHIVED, BLOCKED
              - column:
                  name: state_reason
                  type: text
                  constraints:
                    nullable: true
              - column:
                  name: created_at
                  type: timestamp
                  defaultValueComputed: "CURRENT_TIMESTAMP"
                  remarks: insert_datetime of the row
              - column:
                  name: updated_at
                  type: timestamp
                  remarks: last_update_datetime of the row
