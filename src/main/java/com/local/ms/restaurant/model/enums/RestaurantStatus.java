package com.local.ms.restaurant.model.enums;

public enum RestaurantStatus {
    OPEN,       // restaurant works
    CLOSED,     // restaurant closed
    BUSY        // restaurant is busy because of any reasons (for example lots of orders)
}
